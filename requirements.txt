###################################################################
# Project Requirements (merged with mcp_requirements.txt on 2025-09-19)
# - Existing unpinned core libs kept unpinned unless a pinned version was
#   provided in mcp_requirements.txt (we adopt that pin where sensible).
# - Duplicates removed. Comments preserved.
###################################################################

# Core CV / ML stack
opencv-python-headless
ultralytics
pillow
numpy

# Testing
pytest

# GeoTIFF / geospatial support
rasterio

# HTTP
requests==2.32.5  # pinned (was unpinned before)
urllib3==2.5.0
certifi==2025.8.3
idna==3.10
charset-normalizer==3.4.3

# FastAPI + server utilities (from MCP requirements)
fastapi==0.115.7
uvicorn==0.34.0
click==8.2.1
colorama==0.4.6
python-dotenv==1.1.1

# Data / analysis
pandas==2.3.2
python-dateutil==2.9.0.post0
pytz==2025.2

# Pydantic / typing
pydantic==2.11.9
pydantic_core==2.33.2
typing_extensions==4.15.0
annotated-types==0.7.0
typing-inspection==0.4.1

# JSON Schema stack
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
referencing==0.36.2
rpds-py==0.27.1

# STAC / planetary computer tools
planetary-computer==1.0.0
pystac==1.14.0
pystac-client==0.9.0

# Azure identity (if remote auth / data access is needed)
azure-identity==1.25.0

# Misc utilities
six==1.17.0
packaging==25.0
typing==3.7.4.3

# MCP tooling
mcp==1.14.0

###################################################################
# End merged list
###################################################################

